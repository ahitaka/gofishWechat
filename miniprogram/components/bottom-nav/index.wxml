<view class="bottom-nav">
  <view wx:for="{{list}}" wx:key="key" class="nav-item {{item.key === 'publish' ? 'prominent' : ''}}" data-path="{{item.pagePath}}" bindtap="switchTab">
    <view class="icon">{{item.icon}}</view>
    <view class="label">{{item.text}}</view>
  </view>
</view>
