# GoFish 项目日志 - 2026-06-21

## 生成测试数据清理 SQL
- 需求：清理用户「路亚林子」、「夜钓老许」、「溪边阿陈」以及他们发布的钓点和帖子
- 实现：
  - 生成 `cleanup_test_users.sql` 脚本，使用临时表按正确顺序删除关联数据
  - 删除范围：用户、钓点、帖子、点赞、评论、收藏、关注、通知、浏览历史、文件记录、举报等
  - 脚本末尾包含计数器刷新，确保 user.post_count/like_count、spot.post_count/favorite_count、post.like_count/comment_count 等保持一致
- 执行方式：在 MySQL 客户端中运行，包含 START TRANSACTION / COMMIT
- 涉及文件：`cleanup_test_users.sql`

## 清理 SQL 执行问题排查
- 用户执行 `cleanup_test_users.sql` 时遇到 MySQL 1064 语法错误，原因为 DBeaver 将 `START TRANSACTION` + `CREATE TEMPORARY TABLE` 整体解析导致
- 生成 `cleanup_test_users_v2.sql`：去掉事务和临时表，改为硬编码用户 ID 并逐条执行
- 用户继续遇到 1054 "Unknown column 'user_id'" 错误，定位为 `fc_browse_history` 表可能未应用 V9 迁移或字段缺失
- 生成 `cleanup_test_users_v3.sql`：去掉 `fc_browse_history`，保留其他清理逻辑，作为兼容版本
