# GoFish 项目日志 - 2026-06-19

## 修复后端依赖 CVE 漏洞
- 问题：IDE 警告 `commons-lang3:3.17.0` 存在 CVE-2025-48924（评分 5.3，未受控递归漏洞）
- 修复：在 `pom.xml` 中通过 `dependencyManagement` 覆盖 `commons-lang3` 版本为 3.18.0（Spring Boot 官方确认已修复该 CVE）
- 验证：`mvn dependency:tree` 显示已升级到 `commons-lang3:3.18.0`，编译成功
- 涉及文件：`/Users/ashitaka/gitRep/gofishbackend/fishing-community-backend/pom.xml`

## 修复 Bouncy Castle CVE 漏洞
- 问题：IDE 警告 `bcprov-jdk18on:1.78.1` 存在 CVE-2026-0636（评分 5.3，LDAP 注入漏洞）
- 来源：由 `io.minio:minio:8.5.17` 传递引入
- 修复：在 `pom.xml` 的 `dependencyManagement` 中覆盖 `bcprov-jdk18on` 版本为 1.84（Snyk 确认已修复）
- 验证：`mvn dependency:tree` 显示已升级到 `bcprov-jdk18on:1.84`，编译成功
- 涉及文件：`/Users/ashitaka/gitRep/gofishbackend/fishing-community-backend/pom.xml`

## 修复 MinIO Java SDK CVE 漏洞
- 问题：IDE 警告 `io.minio:minio:8.5.17` 存在 CVE-2025-59952（评分 7.5，XML 标签值替换导致信息泄露）
- 修复：升级 `minio` 到 8.6.0（GitHub 官方确认已修复）
- 兼容性处理：MinIO 8.6.0 默认传递 `okhttp 5.1.0`（alpha 版，包名不兼容），通过 `dependencyManagement` 强制覆盖为 `okhttp 4.12.0`
- 验证：`mvn dependency:tree` 显示 `minio:8.6.0`、`okhttp:4.12.0`、`bcprov-jdk18on:1.84`、`commons-lang3:3.18.0`，编译成功
- 涉及文件：`/Users/ashitaka/gitRep/gofishbackend/fishing-community-backend/pom.xml`
